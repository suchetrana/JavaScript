show databases;
CREATE DATABASE BACKEND;
USE BACKEND;

CREATE TABLE first
(
Name varchar(15),
Dob DATE,
Salary DECIMAL
);

SHOW TABLES;

DESC FIRST;

INSERT INTO FIRST (Name, Dob, Salary) VALUES("SANKET", "1998-12-24", 100000000);
-- SQL STORE DATE IN YYYY-MM-DD FORMAT

SELECT * FROM FIRST;

CREATE TABLE SECOND
(
Name varchar(15) NOT NULL,
Dob DATE DEFAULT "2000-01-01",
Gender ENUM("MALE", "FEMALE", "OTHERS")
);

DESC SECOND;

INSERT INTO SECOND (NAME, DOB, GENDER) VALUES("SUCHET", DEFAULT, "MALE");

SELECT * FROM SECOND;

